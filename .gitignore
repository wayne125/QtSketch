# Build outputs
build/
build*/
*.user
*.user.*

# Runtime artifacts
qml_errors.log
crash.log
error.log
error.txt
run_log.txt
*.log

# Scratch / experiments
scratch/
src/test_*.json
run_sketch.bat

# Vendored Indigo SDK (637 MB third-party tree; kept on disk, not in history.
# See THIRD-PARTY-NOTICES.md — source: https://github.com/epam/indigo)
indigo/
# Separate full Indigo source-tree checkout (distinct from indigo/ above) -
# accidentally committed via a broad `git add` during an agy-delegate task;
# untracked here, kept on disk locally only. Includes >50MB font blobs.
Indigo-indigo-1.45.0/
# Duplicate/unused copy of the Indigo runtime DLL at repo root — the build
# links and copies indigo/lib/indigo.dll instead (see CMakeLists.txt).
/indigo.dll

# Official EPAM MSVC build of Indigo (indigo-libs-windows-x86_64, v1.43.0) —
# same "vendored binary blobs, kept on disk not in history" treatment as
# indigo/ above. See CMakeLists.txt's MSVC branch.
/indigo-libs-windows-x86_64/
/indigo-libs-windows-x86_64.zip

# Vendored Imago SDK (imago.dll + consumer header) — built fresh from source
# (github.com/epam/Imago, Apache-2.0), same "kept on disk, not in history"
# treatment as indigo/ above. See CMakeLists.txt and Architecture Map.md.
imago/

# Editor / tool-specific state (not needed to build)
.idea/
.cursor/
.antigravitycli/
.qtcreator/
.claude/
.codegraph/
.superpowers/

# Ad-hoc analysis writeups (not needed to build) - kept on disk locally only.
docs/analysis/

# Session planning docs / notes (not needed to build)
docs/superpowers/
velvety-meandering-snowglobe.md
ui-typography-redesign.md

# Other-editor AI agent instruction files (not used by this project's own workflow) -
# untracked from git, kept on disk locally only.
.clinerules/
.windsurf/
.opencode/
.github/
AGENTS.md

# Feature-tracking/planning docs - untracked from git, kept on disk locally only.
Architecture Map.md
Architecture Map.html
Features To Be Implemented.md
library-audit.html

# Journal/ChemDraw template samples - untracked from git, kept on disk locally only.
Journal Templates/

# Retired JS engine (quickjs-ng vendor tree + qjs_engine.*/chem-core.js) - kept on disk
# for local reference only, no longer tracked/pushed. Same "on disk, not in history"
# treatment as indigo/ and imago/ above.
archive/js-engine/
